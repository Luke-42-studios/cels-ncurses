---
phase: 08-sub-cell-rendering
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - examples/draw_test.c
autonomous: false

must_haves:
  truths:
    - "draw_test scene shows half-block filled rectangles with independent top/bottom colors"
    - "draw_test scene shows quadrant pixels forming 2x2 resolution patterns"
    - "draw_test scene shows braille dots composing into recognizable shapes"
    - "draw_test scene shows resolution query output for all three modes"
    - "draw_test builds and runs without crashes"
    - "Full project (app target) builds without errors"
  artifacts:
    - path: "examples/draw_test.c"
      provides: "Scene 8 sub-cell rendering demo with all three modes"
      contains: "tui_draw_halfblock"
  key_links:
    - from: "examples/draw_test.c"
      to: "include/cels-ncurses/tui_draw.h"
      via: "calls all sub-cell draw functions"
      pattern: "tui_draw_halfblock_fill_rect\\|tui_draw_quadrant_plot\\|tui_draw_braille_plot"
    - from: "examples/draw_test.c"
      to: "include/cels-ncurses/tui_subcell.h"
      via: "uses TUI_SubCellMode enum for resolution query"
      pattern: "TUI_SUBCELL_HALFBLOCK\\|TUI_SUBCELL_QUADRANT\\|TUI_SUBCELL_BRAILLE"
---

<objective>
Add a sub-cell rendering demo scene to the draw_test example and verify the full project builds.

Purpose: Provide visual proof that all three sub-cell modes work correctly (half-block, quadrant, braille), demonstrate the resolution query function, and confirm the full project builds with the new code integrated. This follows the same pattern used in Phase 7 (07-02-PLAN.md) where draw_test was updated with a color system demo.

Output: New scene 8 in draw_test.c exercising all sub-cell draw functions, plus a human-verified checkpoint confirming visual correctness.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sub-cell-rendering/08-CONTEXT.md
@.planning/phases/08-sub-cell-rendering/08-RESEARCH.md
@.planning/phases/08-sub-cell-rendering/08-01-SUMMARY.md
@.planning/phases/08-sub-cell-rendering/08-02-SUMMARY.md
@examples/draw_test.c
@include/cels-ncurses/tui_draw.h
@include/cels-ncurses/tui_subcell.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sub-cell rendering scene to draw_test</name>
  <files>examples/draw_test.c</files>
  <action>
    Update draw_test.c to add Scene 8 (Sub-Cell Rendering). Follow the existing scene pattern exactly.

    **Changes to existing code:**

    1. Add `#include <cels-ncurses/tui_subcell.h>` to the includes (for TUI_SubCellMode enum).

    2. Change `#define SCENE_COUNT 7` to `#define SCENE_COUNT 8`.

    3. Add "8. Sub-Cell Rendering" to the `scene_names[]` array in `draw_menu()`.

    4. Update the menu footer and input handling: accept '8' key (the existing `ch >= '1' && ch <= '7'` check should become `ch <= '8'`).

    **New scene function -- `draw_scene_subcell()`:**

    The scene layout has three vertical sections, one for each mode:

    **Section 1: Half-Block Demo (top)**
    - Title: "Half-Block Mode (1xN, 2x vertical resolution)"
    - Draw several overlapping half-block filled rectangles with different colors:
      - A red rectangle (e.g., px=2, py=0, pw=20, ph=6 -- in pixel coords, so 3 cells tall)
      - A blue rectangle overlapping partially (px=10, py=3, pw=20, ph=6)
      - A green single-pixel row using tui_draw_halfblock_plot in a loop
    - This demonstrates: independent top/bottom colors, color preservation when halves overlap, fill_rect at 2x resolution.

    **Section 2: Quadrant Demo (middle)**
    - Title: "Quadrant Mode (2x2 resolution per cell)"
    - Draw a small checkerboard pattern using tui_draw_quadrant_plot, alternating two colors
    - Draw a quadrant fill_rect forming a diagonal stripe pattern
    - This demonstrates: 2x2 resolution, quadrant character selection, two-color per cell.

    **Section 3: Braille Demo (bottom)**
    - Title: "Braille Mode (2x4 resolution per cell)"
    - Draw a sine wave using tui_draw_braille_plot in a loop: `for (int px = 0; px < braille_w; px++) { int py = center_y + (int)(sinf(px * 0.15f) * amplitude); tui_draw_braille_plot(...); }`
    - Draw a small braille fill_rect (a solid block of dots)
    - Demonstrate unplot: draw a filled rect, then unplot a few dots in the middle to create a hole
    - This demonstrates: dot compositing, sine curve at 2x4 resolution, unplot functionality.

    **Section 4: Resolution Query (sidebar or bottom)**
    - Call `tui_draw_subcell_resolution(ctx, mode, &w, &h)` for each mode
    - Display: "Half-block: WxH pixels", "Quadrant: WxH pixels", "Braille: WxH pixels"
    - Use the HUD pattern for display.

    **Add to scene dispatch:**
    In the main loop's switch statement, add: `case 8: draw_scene_subcell(&ctx, cols, rows); break;`

    **Style colors for the demo:**
    Use tui_color_rgb for vivid colors:
    - Half-block: red (220,50,50), blue (50,100,220), green (50,200,80)
    - Quadrant: yellow (230,200,50), purple (160,80,220)
    - Braille: cyan (80,220,220) for dots

    **Build verification:**
    After adding the scene, build both targets:
    - `cd /home/cachy/workspaces/libs/cels && cmake --build build 2>&1` (builds both draw_test and app)
    - Must compile with zero errors.
  </action>
  <verify>
    Build: `cd /home/cachy/workspaces/libs/cels && cmake --build build 2>&1`
    Must compile with zero errors for both targets.
    Run: `./build/modules/cels-ncurses/draw_test` (or equivalent path) -- navigate to scene 8, verify it renders without crash. (Visual correctness verified in checkpoint below.)
  </verify>
  <done>
    - draw_test has Scene 8 with sub-cell rendering demos
    - All three modes are demonstrated: half-block rects, quadrant pixels, braille dots
    - Resolution query output is displayed
    - Both build targets compile without errors
    - draw_test runs and scene 8 renders without crashes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 8 sub-cell rendering system:
    - Shadow buffer infrastructure (types, lifecycle, frame/layer integration)
    - Half-block rendering at 2x vertical resolution
    - Quadrant rendering at 2x2 resolution with two-color constraint
    - Braille rendering at 2x4 resolution with dot compositing
    - Resolution query function
    - Interactive demo scene in draw_test
  </what-built>
  <how-to-verify>
    1. Build: `cd /home/cachy/workspaces/libs/cels && cmake --build build`
    2. Run: `./build/modules/cels-ncurses/draw_test`
    3. Press '8' to enter the Sub-Cell Rendering scene
    4. Verify visually:
       a. Half-block section: colored rectangles at double vertical resolution, overlapping areas show correct color merging (one half preserves the other)
       b. Quadrant section: 2x2 pixel patterns with correct Unicode quadrant characters
       c. Braille section: sine wave curve rendered as braille dots, solid braille rectangle, and a braille rect with dots "unplotted" in the middle
       d. Resolution info is displayed (e.g., for an 80x24 terminal: halfblock=80x48, quadrant=160x48, braille=160x96)
    5. Press Escape to return to menu, verify no visual artifacts
    6. Press 'q' to quit cleanly
  </how-to-verify>
  <resume-signal>Type "approved" if all three modes render correctly, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
- `cd /home/cachy/workspaces/libs/cels && cmake --build build 2>&1` compiles with zero errors
- `grep -n "draw_scene_subcell\|SCENE_COUNT 8\|Sub-Cell" examples/draw_test.c` shows scene integration
- `grep -n "tui_draw_halfblock\|tui_draw_quadrant\|tui_draw_braille\|tui_draw_subcell_resolution" examples/draw_test.c` shows all sub-cell functions used
- draw_test runs, scene 8 accessible via '8' key
- Human verification confirms visual correctness of all three modes
</verification>

<success_criteria>
1. draw_test Scene 8 exercises all 8 sub-cell functions (2 halfblock + 2 quadrant + 3 braille + 1 resolution)
2. Half-block rectangles show correct 2x vertical resolution with color preservation
3. Quadrant patterns show correct 2x2 resolution with proper Unicode characters
4. Braille dots compose correctly into sine wave and show unplot functionality
5. Resolution query displays correct dimensions
6. Full project builds (both draw_test standalone and cels-ncurses INTERFACE via app)
7. Human confirms visual output is correct
</success_criteria>

<output>
After completion, create `.planning/phases/08-sub-cell-rendering/08-03-SUMMARY.md`
</output>
