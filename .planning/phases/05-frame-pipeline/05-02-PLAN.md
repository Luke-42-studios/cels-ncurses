---
phase: 05-frame-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/frame/tui_frame.c
  - src/layer/tui_layer.c
  - examples/draw_test.c
  - include/cels_ncurses_draw.h
  - src/tui_internal.h
  - CMakeLists.txt
autonomous: true

must_haves:
  truths:
    - "Legacy tui_frame.c is deleted -- no file exists at src/frame/tui_frame.c"
    - "Legacy tui_layer.c is deleted -- no file exists at src/layer/tui_layer.c"
    - "draw_test example is deleted -- no file exists at examples/draw_test.c"
    - "Public header has no legacy layer/frame declarations"
    - "CMakeLists has no references to deleted files or draw_test target"
    - "minimal target builds cleanly with cmake and make"
  artifacts:
    - path: "include/cels_ncurses_draw.h"
      provides: "Drawing API without legacy layer/frame sections"
    - path: "CMakeLists.txt"
      provides: "Build config without tui_layer.c, tui_frame.c, draw_test target"
    - path: "src/tui_internal.h"
      provides: "Internal header without ncurses_register_frame_systems declaration"
  key_links:
    - from: "CMakeLists.txt target_sources"
      to: "actual files in src/"
      via: "INTERFACE sources list"
      pattern: "target_sources.*INTERFACE"
    - from: "include/cels_ncurses_draw.h"
      to: "no g_layers, g_layer_count, g_frame_state externs"
      via: "header declarations"
      pattern: "g_layers|g_layer_count|g_frame_state"
---

<objective>
Delete legacy files and strip all legacy layer/frame API surface from headers, CMake, and internal declarations.

Purpose: Complete the migration from imperative frame/layer API to entity-driven ECS systems. Remove all dead code that would cause link errors or API confusion.

Output: Three files deleted (tui_frame.c, tui_layer.c, draw_test.c), headers cleaned, CMake updated, build verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frame-pipeline/05-RESEARCH.md
@.planning/phases/05-frame-pipeline/05-CONTEXT.md
@.planning/phases/05-frame-pipeline/05-01-SUMMARY.md

@include/cels_ncurses_draw.h
@src/tui_internal.h
@CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy files and clean headers</name>
  <files>
    src/frame/tui_frame.c
    src/layer/tui_layer.c
    examples/draw_test.c
    include/cels_ncurses_draw.h
    src/tui_internal.h
    src/tui_window.h
  </files>
  <action>
  **Delete files:**
  - `rm src/frame/tui_frame.c` (241 lines, logic absorbed into ncurses_module.c)
  - `rm src/layer/tui_layer.c` (223 lines, legacy imperative API replaced by entity layers)
  - `rm examples/draw_test.c` (~1200 lines, standalone non-ECS demo no longer relevant)
  - `rmdir src/frame/` (directory should be empty after tui_frame.c deletion)

  **Clean include/cels_ncurses_draw.h:**
  Remove the entire "Layer System" section (lines ~540-584) which contains:
  - `#define TUI_LAYER_MAX 32` (this is now only in ncurses_module.c)
  - `typedef struct TUI_Layer { ... }` (no consumers remain)
  - `extern TUI_Layer g_layers[TUI_LAYER_MAX]` and `extern int g_layer_count`
  - All `tui_layer_*` function declarations (create, destroy, show, hide, raise, lower, move, resize, resize_all, get_draw_context)

  Remove the entire "Frame Pipeline" section (lines ~586-607) which contains:
  - `typedef struct TUI_FrameState { ... }` (redundant with NCurses_WindowState)
  - `extern TUI_FrameState g_frame_state`
  - `tui_frame_init`, `tui_frame_begin`, `tui_frame_end`, `tui_frame_invalidate_all`, `tui_frame_get_background` declarations
  - `#ifdef CELS_HAS_ECS` / `tui_frame_register_systems` / `#endif`

  Keep everything else in cels_ncurses_draw.h intact (rects, colors, styles, draw context, subcell, drawing primitives, scissor).

  **Clean src/tui_internal.h:**
  Remove the line `extern void ncurses_register_frame_systems(void);` (line 52). The frame systems are now registered directly in the module init via cels_register.

  **Clean src/tui_window.h:**
  This file can stay as-is. `tui_hook_frame_end()` declaration is still needed because ncurses_module.c now includes tui_window.h and calls tui_hook_frame_end() from TUI_FrameEndSystem. The WindowState enum is internal implementation detail for tui_window.c.
  </action>
  <verify>
  `test ! -f src/frame/tui_frame.c && echo "deleted"` should print "deleted".
  `test ! -f src/layer/tui_layer.c && echo "deleted"` should print "deleted".
  `test ! -f examples/draw_test.c && echo "deleted"` should print "deleted".
  `test ! -d src/frame/ && echo "deleted"` should print "deleted".
  `grep -c "g_layers\|g_layer_count\|g_frame_state\|tui_frame_init\|tui_frame_begin\|tui_frame_end\|tui_layer_create\|tui_layer_destroy\|TUI_FrameState\|typedef struct TUI_Layer" include/cels_ncurses_draw.h` should return 0.
  `grep -c "ncurses_register_frame_systems" src/tui_internal.h` should return 0.
  </verify>
  <done>
  Three legacy files deleted. The src/frame/ directory removed. cels_ncurses_draw.h has no legacy layer/frame API. tui_internal.h has no frame system registration declaration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CMakeLists.txt and verify build</name>
  <files>CMakeLists.txt</files>
  <action>
  **Update target_sources for cels-ncurses INTERFACE library:**
  Remove these two lines from the target_sources block:
  - `${CMAKE_CURRENT_SOURCE_DIR}/src/layer/tui_layer.c`
  - `${CMAKE_CURRENT_SOURCE_DIR}/src/frame/tui_frame.c`

  Keep `${CMAKE_CURRENT_SOURCE_DIR}/src/layer/tui_layer_panel.c` (still needed for entity layer helpers).

  **Delete the entire draw_test target block** (lines ~108-116):
  ```cmake
  # Drawing primitives visual test (standalone, no CELS framework)
  add_executable(draw_test
      examples/draw_test.c
      src/graphics/tui_color.c src/graphics/tui_draw.c
      src/graphics/tui_scissor.c src/graphics/tui_subcell.c
      src/layer/tui_layer.c src/frame/tui_frame.c)
  target_include_directories(draw_test PRIVATE include ${CURSES_INCLUDE_DIRS})
  target_compile_definitions(draw_test PRIVATE _XOPEN_SOURCE=700 _DEFAULT_SOURCE)
  target_link_libraries(draw_test PRIVATE ${CURSES_LIBRARIES} ${PANEL_LIBRARY} m)
  set_target_properties(draw_test PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED ON)
  ```

  Also add `${CMAKE_CURRENT_SOURCE_DIR}/src` as an INTERFACE include directory (if not already present) so that ncurses_module.c can `#include "tui_window.h"` for the tui_hook_frame_end declaration. Check first -- if src/ is already in target_include_directories, skip this. Looking at current CMakeLists: src/ IS already present as an INTERFACE include (line 81 area references `${CURSES_INCLUDE_DIRS}` but not src/). Actually, the existing code has `#include "tui_window.h"` working in tui_frame.c via relative path `"../tui_window.h"`. Since ncurses_module.c is in src/ and tui_window.h is also in src/, the include should be `#include "tui_window.h"` and it should resolve because they are in the same directory. No CMake include path change needed.

  **Build verification:**
  Run the build using the wrapper build approach from prior phases:
  ```bash
  cd /tmp && rm -rf cels-ncurses-build && mkdir cels-ncurses-build && cd cels-ncurses-build
  cmake /home/cachy/workspaces/libs/cels-ncurses -DCMAKE_BUILD_TYPE=Debug 2>&1
  make minimal 2>&1
  ```
  The `minimal` target must compile and link with zero errors.

  If build fails due to `tui_window.h` include path, fix by using the path that works (e.g., `"tui_window.h"` if same directory, or add src/ to include dirs).
  </action>
  <verify>
  `grep -c "tui_layer\.c\|tui_frame\.c\|draw_test" CMakeLists.txt` should return 0.
  Build completes: `make minimal` exits 0 with no errors.
  </verify>
  <done>
  CMakeLists.txt references only existing source files. draw_test target removed. The minimal target builds cleanly, proving the frame pipeline is correctly wired through ECS systems with no unresolved symbols.
  </done>
</task>

</tasks>

<verification>
- `ls src/frame/tui_frame.c src/layer/tui_layer.c examples/draw_test.c 2>&1` returns "No such file" for all three
- `src/frame/` directory does not exist
- `grep -c "g_layers\|g_frame_state\|tui_layer_create\|tui_frame_begin" include/cels_ncurses_draw.h` returns 0
- `grep -c "tui_layer\.c\|tui_frame\.c\|draw_test" CMakeLists.txt` returns 0
- `make minimal` in build directory exits 0
- No unresolved symbol errors at link time
</verification>

<success_criteria>
All legacy frame/layer code is deleted from the codebase.
Public and internal headers expose only the entity-driven API.
CMake builds the minimal target successfully with no dead file references.
The frame pipeline is fully entity-driven with zero legacy remnants.
</success_criteria>

<output>
After completion, create `.planning/phases/05-frame-pipeline/05-02-SUMMARY.md`
</output>
