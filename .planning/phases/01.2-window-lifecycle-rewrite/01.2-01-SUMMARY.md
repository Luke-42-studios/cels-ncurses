---
phase: 01.2-window-lifecycle-rewrite
plan: 01
subsystem: api
tags: [cels, ecs, flecs, component-api, entity-set-get]

# Dependency graph
requires:
  - phase: 01.1-cels-api-purge
    provides: "Isolated flecs API calls to bridge file; identified need for entity component set/get"
provides:
  - "cels_entity_set_component() public API in CELS"
  - "cels_entity_get_component() public API in CELS"
affects:
  - 01.2-window-lifecycle-rewrite (plan 02 uses these APIs to eliminate flecs.h from cels-ncurses)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Entity component set/get outside composition context via cels_entity_set/get_component"

key-files:
  created: []
  modified:
    - "/home/cachy/workspaces/libs/cels/include/cels/private/cels_runtime.h"
    - "/home/cachy/workspaces/libs/cels/src/cels_api.c"

key-decisions:
  - "cels_entity_set_component wraps ecs_set_id with null-guard and world accessor pattern"
  - "cels_entity_get_component wraps ecs_get_id, returns const void* (NULL if missing)"
  - "Functions placed in Component State Notify section, after cels_component_notify_change"
  - "Work done in CELS repo (v0.5.2 branch), not cels-ncurses repo"

patterns-established:
  - "Entity-targeted component mutation: cels_entity_set_component(entity, comp_id, data, size) for setting component data outside composition context"

# Metrics
duration: 2min
completed: 2026-03-01
---

# Phase 1.2 Plan 01: Entity Component Get/Set API Summary

**Two new CELS public API functions (cels_entity_set/get_component) wrapping ecs_set_id/ecs_get_id for flecs-free entity component mutation**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-01T20:28:27Z
- **Completed:** 2026-03-01T20:30:01Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added `cels_entity_set_component()` to CELS public API -- sets arbitrary component data on any entity by ID
- Added `cels_entity_get_component()` to CELS public API -- reads component data from any entity by ID
- All 95 CELS tests pass with zero regressions
- Enables cels-ncurses Phase 1.2 Plan 02 to eliminate `#include <flecs.h>` from all source files

## Task Commits

Each task was committed atomically:

1. **Task 1: Declare entity component get/set in cels_runtime.h** - `19f0a1a` (feat)
2. **Task 2: Implement entity component get/set in cels_api.c** - `b48bb86` (feat)

## Files Created/Modified
- `include/cels/private/cels_runtime.h` - Added extern declarations for cels_entity_set_component and cels_entity_get_component
- `src/cels_api.c` - Added implementations wrapping ecs_set_id and ecs_get_id with null-guard and world accessor patterns

## Decisions Made
- Placed declarations after `cels_component_notify_change` in the component registration section of cels_runtime.h, keeping related functions grouped
- Used the `cels_world_manager_world()` pattern (not `cels_get_world()`) consistent with all other functions in cels_api.c
- Used `cels_context == NULL` null-guard return pattern consistent with every function in cels_api.c
- Target was the `v0.5.2` branch in CELS (where Phase 30.1 lifecycle hooks were completed), not `v0.6.0`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Test target name was test_cels not cels_tests**
- **Found during:** Task 2 verification
- **Issue:** Plan specified `cels_tests` as CMake target but actual target is `test_cels`
- **Fix:** Used correct target name `test_cels`
- **Verification:** Build and tests succeeded (95/95 pass)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Minor -- wrong target name in plan, no code impact.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Both `cels_entity_set_component` and `cels_entity_get_component` are available in the CELS public API
- cels-ncurses Plan 02 can now replace `ecs_set_id`/`ecs_get_id` calls with these CELS wrappers
- Success criterion 7 ("No `#include <flecs.h>` in any cels-ncurses source file") is now achievable

---
*Phase: 01.2-window-lifecycle-rewrite*
*Completed: 2026-03-01*
