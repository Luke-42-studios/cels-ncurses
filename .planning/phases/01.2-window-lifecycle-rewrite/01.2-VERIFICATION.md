---
phase: 01.2-window-lifecycle-rewrite
verified: 2026-03-01T20:43:45Z
status: passed
score: 10/10 must-haves verified
---

# Phase 1.2: Window Lifecycle Rewrite Verification Report

**Phase Goal:** Replace the ECS bridge file and raw flecs observers with the new CEL_Lifecycle + CEL_Observe pattern. Window init/shutdown becomes lifecycle-driven. Composition declares components, observers handle side effects, systems update per-frame state. No flecs.h in any cels-ncurses file.
**Verified:** 2026-03-01T20:43:45Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `ncurses_ecs_bridge.c` and `ncurses_ecs_bridge.h` deleted -- no bridge file exists | VERIFIED | `ls src/ncurses_ecs_bridge.*` returns "No such file or directory". Zero matches for `ncurses_ecs_bridge` across all `.c`, `.h`, and CMake files. |
| 2 | `NCursesWindowLC` lifecycle defined with `.active` condition driving window active/suspended state | VERIFIED | `src/ncurses_module.c:64` has `CEL_Lifecycle(NCursesWindowLC, .active = &NCursesActive);` and line 60 has `CEL_Condition(NCursesActive) { return ncurses_window_is_active(); }` |
| 3 | `CEL_Observe(NCursesWindowLC, on_create)` calls `ncurses_terminal_init()` -- no component setting in observer | VERIFIED | `src/ncurses_module.c:66-83` shows on_create calls only `ncurses_window_set_entity(entity)` and `ncurses_terminal_init(config)`. Zero `cels_entity_set_component` calls in ncurses_module.c. |
| 4 | `CEL_Observe(NCursesWindowLC, on_destroy)` calls `ncurses_terminal_shutdown()` | VERIFIED | `src/ncurses_module.c:85-89` shows on_destroy calls `ncurses_terminal_shutdown()` and `ncurses_window_set_entity(0)`. |
| 5 | `NCursesWindow` composition declares both `NCurses_WindowConfig` and `NCurses_WindowState` via `cel_has` and attaches lifecycle via `cels_lifecycle_bind_entity()` | VERIFIED | `src/ncurses_module.c:128-147`: CEL_Compose(NCursesWindow) has `cel_has(NCurses_WindowConfig, ...)` at line 129, `cel_has(NCurses_WindowState, ...)` at line 136, and `cels_lifecycle_bind_entity(NCursesWindowLC_id, cels_get_current_entity())` at line 146. |
| 6 | Frame system updates `NCurses_WindowState` per-frame with `cels_entity_set_component` paired with `cels_component_notify_change`; `cel_watch` triggers recomposition on actual state changes | VERIFIED | `src/window/tui_window.c` has exactly 2 `cels_entity_set_component` calls (lines 190 and 216) each immediately followed by `cels_component_notify_change(NCurses_WindowState_id)` (lines 192 and 218). Updates happen on quit (line 182-194) and resize (line 208-219) only -- not every frame. |
| 7 | Every `cels_entity_set_component` call in tui_window.c is followed by `cels_component_notify_change` | VERIFIED | 2 set calls and 2 notify calls. Line 190-192 (quit path) and line 216-218 (resize path) are correctly paired. |
| 8 | No `#include <flecs.h>` in any cels-ncurses source file | VERIFIED | `grep -r "flecs.h" src/ include/` returns zero matches across all source and header files. |
| 9 | Minimal example uses `cel_watch(win, NCurses_WindowState)` for reactive window state | VERIFIED | `examples/minimal.c:39` has `const NCurses_WindowState* state = cel_watch(win, NCurses_WindowState);` |
| 10 | Both minimal and draw_test targets build successfully | VERIFIED | `cmake --build build --target minimal` and `cmake --build build --target draw_test` both complete with exit code 0. |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/ncurses_module.c` | CEL_Module + CEL_Lifecycle + CEL_Observe + CEL_Compose | VERIFIED | 165 lines. Has CEL_Condition(NCursesActive), CEL_Lifecycle(NCursesWindowLC), CEL_Observe on_create/on_destroy, CEL_Module(NCurses), CEL_Compose(NCursesWindow). No flecs.h. Exported via CEL_Module. |
| `src/window/tui_window.c` | Terminal init/shutdown, frame update with CELS API | VERIFIED | 256 lines. ncurses_terminal_init(), ncurses_terminal_shutdown(), ncurses_window_frame_update() with cels_entity_set_component + cels_component_notify_change. Includes cels/cels.h only. |
| `include/cels-ncurses/tui_internal.h` | Internal declarations for lifecycle accessors | VERIFIED | 49 lines. Declares ncurses_terminal_init, ncurses_terminal_shutdown, ncurses_window_set_entity, ncurses_window_get_entity, ncurses_window_is_active, plus system registration. |
| `include/cels-ncurses/tui_ncurses.h` | Public component types and composition | VERIFIED | 117 lines. CEL_Component(NCurses_WindowConfig), CEL_Component(NCurses_WindowState), CEL_Define(NCursesWindow), NCursesWindow() call macro. |
| `include/cels-ncurses/tui_window.h` | Internal window header | VERIFIED | 58 lines. Declares tui_window_get_running_ptr, ncurses_window_frame_update, tui_hook_frame_end. |
| `examples/minimal.c` | Demonstrates lifecycle-driven window with cel_watch | VERIFIED | 52 lines. Uses NCursesWindow(.title = "Phase 1.2 Lifecycle Test"), cel_watch(win, NCurses_WindowState), full cels_session loop. |
| `CMakeLists.txt` | No bridge source, no flecs link, no src/ include dir | VERIFIED | No ncurses_ecs_bridge.c in target_sources. No flecs::flecs_static link block. No src/ in include_directories. Links cels (which provides flecs internally). |
| `src/ncurses_ecs_bridge.c` | DELETED | VERIFIED | File does not exist. |
| `src/ncurses_ecs_bridge.h` | DELETED | VERIFIED | File does not exist. |
| `/home/cachy/workspaces/libs/cels/include/cels/private/cels_runtime.h` | cels_entity_set/get_component declarations | VERIFIED | Lines 61-68 declare both functions with documentation. |
| `/home/cachy/workspaces/libs/cels/src/cels_api.c` | cels_entity_set/get_component implementations | VERIFIED | Lines 534-545 implement both functions wrapping ecs_set_id/ecs_get_id with null-guard pattern. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| ncurses_module.c (on_create) | tui_window.c (ncurses_terminal_init) | extern function call | WIRED | on_create at line 82 calls `ncurses_terminal_init(config)`, declared in tui_internal.h line 36, implemented in tui_window.c line 108. |
| ncurses_module.c (on_destroy) | tui_window.c (ncurses_terminal_shutdown) | extern function call | WIRED | on_destroy at line 87 calls `ncurses_terminal_shutdown()`, declared in tui_internal.h line 37, implemented in tui_window.c line 150. |
| tui_window.c (frame update) | cels_entity_set_component + notify | CELS public API | WIRED | Lines 190-192 and 216-218 call cels_entity_set_component followed by cels_component_notify_change. |
| ncurses_module.c (CEL_Compose) | cels_lifecycle_bind_entity | CELS lifecycle API | WIRED | Line 146 calls `cels_lifecycle_bind_entity(NCursesWindowLC_id, cels_get_current_entity())`. NCursesWindowLC_id is a static variable from CEL_Lifecycle in the same TU. |
| ncurses_module.c (CEL_Compose) | cel_has(NCurses_WindowState) | composition declaration | WIRED | Line 136 declares `cel_has(NCurses_WindowState, ...)` with initial placeholder values. |
| minimal.c | cel_watch(win, NCurses_WindowState) | CELS reactivity | WIRED | Line 39 uses `cel_watch(win, NCurses_WindowState)` which triggers recomposition when frame update calls notify_change. |
| ncurses_module.c (on_create) | cels_entity_get_component | CELS entity API | WIRED | Line 75 reads config via `cels_entity_get_component(entity, NCurses_WindowConfig_id)`. Implemented in cels_api.c line 541. |

### Requirements Coverage

Phase 1.2 is an inserted sub-phase without direct REQUIREMENTS.md mappings. All 9 success criteria from ROADMAP.md Phase 1.2 section are satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| src/ncurses_module.c | 134 | Comment says "placeholder" | Info | Refers to design pattern (initial zeros overwritten by first frame), not an actual stub. Correct behavior. |

### Human Verification Required

### 1. Window Terminal Init/Shutdown Lifecycle

**Test:** Run `./build/minimal` (or via `cmake --build build --target run_minimal`). Verify ncurses terminal initializes (blank screen, no cursor). Press 'q' or Ctrl+C. Verify clean terminal restoration.
**Expected:** Terminal enters ncurses mode on launch, exits cleanly on quit with no artifacts.
**Why human:** Cannot verify terminal visual state or clean shutdown programmatically from build check alone.

### 2. Resize Reactivity

**Test:** Run minimal example. Resize the terminal window.
**Expected:** `cel_watch` triggers recomposition with updated width/height. No crash on resize.
**Why human:** SIGWINCH handling and ncurses COLS/LINES update require actual terminal resize event.

### Gaps Summary

No gaps found. All 10 must-haves verified against the actual codebase:

- Bridge files are confirmed deleted with zero references remaining in source, headers, or CMake.
- CEL_Lifecycle(NCursesWindowLC) is properly defined with an active condition backed by ncurses_window_is_active().
- on_create observer correctly calls only ncurses_terminal_init() and ncurses_window_set_entity() -- no component setting.
- on_destroy observer calls ncurses_terminal_shutdown().
- CEL_Compose(NCursesWindow) declares both components via cel_has and binds lifecycle via cels_lifecycle_bind_entity().
- Frame system correctly pairs all cels_entity_set_component calls with cels_component_notify_change (2 of each, only on quit/resize).
- Zero flecs.h includes across all cels-ncurses source and header files.
- Minimal example uses cel_watch for reactive state.
- Both build targets compile cleanly.
- CELS prerequisite API (cels_entity_set/get_component) exists in both declaration and implementation.

---

_Verified: 2026-03-01T20:43:45Z_
_Verifier: Claude (gsd-verifier)_
