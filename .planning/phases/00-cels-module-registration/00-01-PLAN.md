---
phase: 00-cels-module-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/cachy/workspaces/libs/cels/scripts/release.sh
autonomous: true

must_haves:
  truths:
    - "Luke-42-studios/cels is the origin remote (dev)"
    - "42-Galaxies/cels is the public remote"
    - "git push origin main pushes to Luke-42-studios"
    - "scripts/release.sh exists and can filter+push to public remote"
  artifacts:
    - path: "scripts/release.sh"
      provides: "Filtered push to public remote"
      contains: "filter-repo\\|archive"
  key_links:
    - from: "scripts/release.sh"
      to: "public remote (42-Galaxies/cels)"
      via: "git push to public"
      pattern: "git.*push.*public"
---

<objective>
Reconfigure the cels repo dual-remote setup and create the release script.

Purpose: Establish the dev/public remote workflow so all subsequent work is captured on Luke-42-studios (dev) and filtered releases go to 42-Galaxies (public). This is a prerequisite for the v0.5.1 release and the standard pattern for all future libs.

Output: Dual-remote configuration in /home/cachy/workspaces/libs/cels, scripts/release.sh committed to dev remote.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/cachy/workspaces/libs/cels-ncurses/.planning/PROJECT.md
@/home/cachy/workspaces/libs/cels-ncurses/.planning/ROADMAP.md
@/home/cachy/workspaces/libs/cels-ncurses/.planning/STATE.md
@/home/cachy/workspaces/libs/cels-ncurses/.planning/phases/00-cels-module-registration/00-CONTEXT.md
@/home/cachy/workspaces/libs/cels-ncurses/.planning/phases/00-cels-module-registration/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconfigure git remotes for dual-remote workflow</name>
  <files>(git config only -- no files created)</files>
  <action>
  All work happens in `/home/cachy/workspaces/libs/cels`.

  1. Rename current origin to public:
     ```bash
     git remote rename origin public
     ```

  2. Add Luke-42-studios as new origin:
     ```bash
     git remote add origin https://github.com/Luke-42-studios/cels.git
     ```

  3. Push all history and tags to the dev remote:
     ```bash
     git push -u origin main
     git push origin --tags
     ```

  4. Verify both remotes are configured:
     ```bash
     git remote -v
     ```
     Expected output should show:
     - `origin` -> `https://github.com/Luke-42-studios/cels.git`
     - `public` -> `https://github.com/42-Galaxies/cels.git`
  </action>
  <verify>
  Run `git remote -v` in `/home/cachy/workspaces/libs/cels` and confirm:
  - origin points to Luke-42-studios/cels
  - public points to 42-Galaxies/cels
  - `git push origin main` succeeds (or is already up to date)
  </verify>
  <done>
  origin = Luke-42-studios/cels (dev), public = 42-Galaxies/cels. Main branch tracks origin. All existing history and tags present on dev remote.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/release.sh for filtered public pushes</name>
  <files>/home/cachy/workspaces/libs/cels/scripts/release.sh</files>
  <action>
  Create `scripts/release.sh` in `/home/cachy/workspaces/libs/cels`. This script creates a filtered push to the public remote, excluding dev artifacts.

  The script must:
  1. Be reusable across libs (cels, cels-ncurses, etc.) -- same dual-remote pattern
  2. Extract version from CMakeLists.txt (`project(... VERSION X.Y.Z ...)`)
  3. Exclude these paths from the public push: `.planning/`, `research/`, `review.md`, `REVIEW_FEEDBACK.md`, `scripts/`
  4. Support `--dry-run` flag that shows what would happen without pushing
  5. Use `set -euo pipefail` for safety

  Implementation approach (Claude has discretion per CONTEXT.md):
  - First check if `git filter-repo` is available (`git filter-repo --version 2>/dev/null`)
  - If available: clone to temp dir, run `git filter-repo --invert-paths --path .planning --path research --path review.md --path REVIEW_FEEDBACK.md --path scripts`, add public remote, force-push
  - If unavailable: use `git archive` approach -- create tarball excluding paths, init temp repo, commit, add public remote, force-push
  - Either way: the temp directory is cleaned up on exit (use `trap`)

  The script should NOT create tags or GitHub releases -- that's a separate step. It only handles the filtered push to public.

  Make the script executable: `chmod +x scripts/release.sh`

  Commit the script to the dev remote:
  ```bash
  git add scripts/release.sh
  git commit -m "feat: add release script for filtered public pushes"
  git push origin main
  ```
  </action>
  <verify>
  1. `ls -la /home/cachy/workspaces/libs/cels/scripts/release.sh` -- file exists and is executable
  2. `bash -n /home/cachy/workspaces/libs/cels/scripts/release.sh` -- no syntax errors
  3. `./scripts/release.sh --dry-run` from the cels repo root -- shows what would be pushed without actually pushing
  </verify>
  <done>
  scripts/release.sh exists, is executable, passes syntax check, and --dry-run shows filtered file list excluding .planning/, research/, review.md, REVIEW_FEEDBACK.md, and scripts/.
  </done>
</task>

</tasks>

<verification>
- `git remote -v` shows origin=Luke-42-studios, public=42-Galaxies
- `scripts/release.sh --dry-run` executes without error and shows the filtering behavior
- Script is committed and pushed to dev remote (origin)
</verification>

<success_criteria>
1. Dual-remote workflow is active: origin (dev) and public (42-Galaxies)
2. Release script exists and handles filtered push to public remote
3. --dry-run confirms dev artifacts (.planning/, research/, review.md, scripts/) are excluded
4. Script committed to dev remote
</success_criteria>

<output>
After completion, create `/home/cachy/workspaces/libs/cels-ncurses/.planning/phases/00-cels-module-registration/00-01-SUMMARY.md`
</output>
